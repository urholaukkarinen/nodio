[package]
name = "nodio-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dependencies.uuid]
version = "1.0.0-alpha.1"
features = ["v4", "serde"]